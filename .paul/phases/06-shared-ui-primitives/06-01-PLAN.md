---
phase: 06-shared-ui-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/shared/ui/Button.tsx
  - app/src/shared/ui/Card.tsx
  - app/src/shared/ui/Input.tsx
  - app/src/shared/ui/Select.tsx
  - app/src/shared/ui/StatusBadge.tsx
  - app/src/shared/ui/index.ts
  - app/src/App.tsx
autonomous: true
---

<objective>
## Goal
Extract the 5 inline UI primitive components from App.tsx into `src/shared/ui/` with a barrel export, then import them back so App.tsx is identical in behavior.

## Purpose
These 5 components (Button, Card, Input, Select, StatusBadge) are used throughout App.tsx and will be imported by every extracted view component in Phases 8–9. They must exist as standalone files before feature extraction begins.

## Output
- 5 component files + 1 barrel in `app/src/shared/ui/`
- App.tsx: inline definitions removed, import added at top
- `npm run build` passes with zero errors
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/05-types-utilities/05-01-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: Shared UI files exist and are correct
```gherkin
Given the shared/ui directory does not exist
When Task 1 completes
Then app/src/shared/ui/ contains Button.tsx, Card.tsx, Input.tsx, Select.tsx, StatusBadge.tsx, index.ts
And each file exports its component with identical implementation to the inline definition in App.tsx
```

## AC-2: App.tsx imports from shared/ui
```gherkin
Given the 6 shared/ui files exist
When Task 2 completes
Then App.tsx has `import { Button, Card, Input, Select, StatusBadge } from './shared/ui'` near the top
And App.tsx no longer contains the inline definitions of those 5 components
```

## AC-3: Build passes, app behavior unchanged
```gherkin
Given App.tsx imports from shared/ui and inline definitions are removed
When `npm run build` runs in the app/ directory
Then it exits 0 with no TypeScript errors
And the app renders identically to before (no visual or behavioral change)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create shared/ui component files</name>
  <files>
    app/src/shared/ui/Button.tsx,
    app/src/shared/ui/Card.tsx,
    app/src/shared/ui/Input.tsx,
    app/src/shared/ui/Select.tsx,
    app/src/shared/ui/StatusBadge.tsx,
    app/src/shared/ui/index.ts
  </files>
  <action>
    Create `app/src/shared/ui/` directory and 6 files.

    Each component file must have `import { cn } from '../../lib/utils';` at the top (path relative to shared/ui/).

    **Button.tsx** — copy exact implementation from App.tsx lines 219–237:
    ```tsx
    import { cn } from '../../lib/utils';

    const Button = ({ className, variant = 'primary', ...props }: any) => {
      const variants: any = {
        primary: 'bg-accent text-white hover:opacity-90 shadow-lg shadow-accent/20',
        secondary: 'bg-zinc-100 text-black hover:bg-zinc-200',
        outline: 'border border-zinc-200 text-black hover:bg-zinc-50',
        ghost: 'text-zinc-600 hover:bg-zinc-100',
        danger: 'bg-red-50 text-red-600 hover:bg-red-100 border border-red-100',
      };
      return (
        <button
          className={cn(
            'px-4 py-2 rounded-xl font-medium transition-all active:scale-95 disabled:opacity-50 disabled:pointer-events-none flex items-center justify-center gap-2',
            variants[variant],
            className
          )}
          {...props}
        />
      );
    };

    export default Button;
    ```

    **Card.tsx** — copy exact implementation from App.tsx lines 239–247:
    ```tsx
    import { cn } from '../../lib/utils';

    const Card = ({ children, className, onClick, style }: any) => (
      <div
        onClick={onClick}
        className={cn('bg-white border border-slate-100 rounded-2xl p-4 shadow-sm hover:shadow-md hover:border-accent/20 transition-all', className)}
        style={style}
      >
        {children}
      </div>
    );

    export default Card;
    ```

    **Input.tsx** — copy exact implementation from App.tsx lines 249–258:
    ```tsx
    import { cn } from '../../lib/utils';

    const Input = ({ label, error, ...props }: any) => (
      <div className="space-y-1.5">
        {label && <label className="text-xs font-semibold text-zinc-500 uppercase tracking-wider">{label}</label>}
        <input
          className="w-full px-4 py-3 bg-zinc-50 border border-zinc-100 rounded-xl focus:outline-none focus:ring-2 focus:ring-accent/5 focus:border-accent transition-all"
          {...props}
        />
        {error && <p className="text-xs text-red-500">{error}</p>}
      </div>
    );

    export default Input;
    ```

    **Select.tsx** — copy exact implementation from App.tsx lines 260–272:
    ```tsx
    import { cn } from '../../lib/utils';

    const Select = ({ label, options, ...props }: any) => (
      <div className="space-y-1.5">
        {label && <label className="text-xs font-semibold text-zinc-500 uppercase tracking-wider">{label}</label>}
        <select
          className="w-full px-4 py-3 bg-zinc-50 border border-zinc-100 rounded-xl focus:outline-none focus:ring-2 focus:ring-accent/5 focus:border-accent transition-all appearance-none"
          {...props}
        >
          {options.map((opt: any) => (
            <option key={opt.value} value={opt.value}>{opt.label}</option>
          ))}
        </select>
      </div>
    );

    export default Select;
    ```

    **StatusBadge.tsx** — copy exact implementation from App.tsx lines 4532–4545:
    ```tsx
    import { cn } from '../../lib/utils';

    function StatusBadge({ status }: { status: string }) {
      const styles: any = {
        'završeno': 'bg-green-100 text-green-600',
        'djelomično završeno': 'bg-sky-100 text-sky-600',
        'čeka materijal': 'bg-orange-100 text-orange-600',
        'blokirano': 'bg-red-100 text-red-600',
        'potrebno dodatno': 'bg-purple-100 text-purple-600',
      };
      return (
        <span className={cn("text-[10px] font-bold uppercase px-2 py-0.5 rounded-full", styles[status])}>
          {status}
        </span>
      );
    }

    export default StatusBadge;
    ```

    **index.ts** — barrel export:
    ```ts
    export { default as Button } from './Button';
    export { default as Card } from './Card';
    export { default as Input } from './Input';
    export { default as Select } from './Select';
    export { default as StatusBadge } from './StatusBadge';
    ```

    Avoid: changing any logic, classNames, prop signatures, or types.
    Avoid: adding React import — Vite's JSX transform handles it automatically (same pattern as Phase 5 utils).
  </action>
  <verify>6 files exist: ls app/src/shared/ui/ shows Button.tsx, Card.tsx, Input.tsx, Select.tsx, StatusBadge.tsx, index.ts</verify>
  <done>AC-1 satisfied: all 6 files created with identical implementations</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx import, remove inline definitions, verify build</name>
  <files>app/src/App.tsx</files>
  <action>
    Follow the extract-and-import pattern: import first, then remove inline.

    **Step 1 — Add import to App.tsx:**
    Find the `import { cn } from './lib/utils';` line (line 111) and add after it:
    ```ts
    import { Button, Card, Input, Select, StatusBadge } from './shared/ui';
    ```

    **Step 2 — Run build to confirm import works before removing anything:**
    `cd app && npm run build`
    If build fails, stop and diagnose — do NOT remove inline definitions until build passes.

    **Step 3 — Remove inline definitions from App.tsx:**
    Remove the block `// --- Components ---` through end of `Select` (lines 217–272 inclusive).
    Remove the `StatusBadge` function definition (lines 4532–4545).

    The `// --- Components ---` comment and the 4 component const blocks should be gone.
    `StatusBadge` function block should be gone.

    **Step 4 — Run build to confirm removal:**
    `cd app && npm run build`
    Must exit 0 with no TypeScript errors.

    Avoid: removing `ErrorBoundary` class (lines 154–215) — it stays in App.tsx for now (Phase 7 scope).
    Avoid: touching any other import, component, or function in App.tsx.
    Avoid: changing the `// --- Main App ---` comment or anything below it.
  </action>
  <verify>npm run build (in app/) exits 0; grep for "const Button" and "function StatusBadge" in App.tsx returns no results</verify>
  <done>AC-2 and AC-3 satisfied: App.tsx imports from shared/ui, inline definitions removed, build passes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `app/src/shared/types/index.ts` — types layer from Phase 5, untouched
- `app/src/shared/utils/` — utilities from Phase 5, untouched
- `app/src/lib/data.ts` — data layer, untouched
- `app/src/lib/utils.ts` — cn utility, untouched
- `ErrorBoundary` class in App.tsx — Phase 7 scope
- All supabase migrations — locked for all v1.1 phases

## SCOPE LIMITS
- No new props, variants, or features added to any component
- No TypeScript type improvements — keep `any` types as-is
- No refactoring of App.tsx beyond removing the 5 inline definitions
- No other files touched (no components outside shared/ui)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `ls app/src/shared/ui/` shows 6 files (Button.tsx Card.tsx Input.tsx Select.tsx StatusBadge.tsx index.ts)
- [ ] `grep -n "const Button" app/src/App.tsx` returns no results
- [ ] `grep -n "function StatusBadge" app/src/App.tsx` returns no results
- [ ] `grep -n "from './shared/ui'" app/src/App.tsx` returns the import line
- [ ] `cd app && npm run build` exits 0
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 6 shared/ui files created with identical implementations to inline originals
- App.tsx imports from shared/ui; 5 inline definitions removed
- npm run build passes with zero errors
- No behavior or visual change to the app
</success_criteria>

<output>
After completion, create `.paul/phases/06-shared-ui-primitives/06-01-SUMMARY.md`
</output>
