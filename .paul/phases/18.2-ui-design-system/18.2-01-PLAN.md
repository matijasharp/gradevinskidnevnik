---
phase: 18.2-ui-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/index.css
autonomous: true
---

<objective>
## Goal
Replace the 4-token @theme stub in `index.css` with the full 15-token design system — updating accent to `#3ab9e3`, defining all text/surface/border/semantic tokens, and removing the Inter font import.

## Purpose
Plans 18.2-02 through 18.2-04 all depend on these tokens existing. Without them, color replacement and sidebar styling in later plans would reference undefined variables. This plan is pure CSS — zero component changes, zero visual regressions beyond the intentional accent color shift from `#6366f1` → `#3ab9e3`.

## Output
`app/src/index.css` with full @theme token system. All existing `bg-accent`, `text-accent`, `shadow-accent` classes automatically reflect the new color.
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@app/src/index.css
@app/index.html
</context>

<acceptance_criteria>

## AC-1: All 15 design tokens defined
```gherkin
Given index.css @theme block is updated
When the file is saved
Then all 15 tokens from the ROADMAP spec are present:
  --color-accent, --color-accent-hover, --color-accent-subtle,
  --color-text-primary, --color-text-secondary, --color-text-muted,
  --color-surface, --color-surface-raised,
  --color-border, --color-border-subtle,
  --color-warning, --color-warning-subtle,
  --color-secondary, --color-error, --color-success
```

## AC-2: Accent color updated and Inter removed
```gherkin
Given the old @theme had --color-accent: #6366f1 and an Inter @import
When Plan 01 is applied
Then --color-accent is #3ab9e3, Inter is removed from the @import URL,
  and Jost is the --font-sans value
```

## AC-3: Build passes
```gherkin
Given index.css is updated
When npm run build runs in /app
Then it exits 0 with no CSS compilation errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Replace @theme stub with full token system in index.css</name>
  <files>app/src/index.css</files>
  <action>
    Replace the entire contents of index.css with:

    ```css
    @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap');
    @import "tailwindcss";
    @plugin "@tailwindcss/typography";

    @theme {
      --font-sans: "Jost", ui-sans-serif, system-ui, sans-serif;
      --font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, monospace;

      /* Accent */
      --color-accent: #3ab9e3;
      --color-accent-hover: #25a8d4;
      --color-accent-subtle: #3ab9e315;

      /* Text */
      --color-text-primary: #192a46;
      --color-text-secondary: #4a5a72;
      --color-text-muted: #8fa0b8;

      /* Surfaces */
      --color-surface: #f1f4f2;
      --color-surface-raised: #ffffff;

      /* Borders */
      --color-border: #e0e6ed;
      --color-border-subtle: #f0f3f5;

      /* Semantic */
      --color-warning: #fad03d;
      --color-warning-subtle: #fad03d20;
      --color-secondary: #816840;
      --color-error: #e05252;
      --color-success: #3dba7a;

      /* Legacy — remove in Plan 18.2-02 after text-primary migrated in CalendarView */
      --color-primary: #0F172A;
    }

    @layer base {
      body {
        @apply font-sans text-slate-900 antialiased bg-surface;
        font-family: 'Jost', sans-serif;
        letter-spacing: 0.015em;
      }
    }
    ```

    Key changes from current file:
    - @import: Remove `Inter` from URL (keep JetBrains Mono only — Jost is already in index.html)
    - --font-sans: "Inter" → "Jost"
    - --color-accent: #6366f1 → #3ab9e3 (plus hover + subtle variants added)
    - --color-surface: #F8FAFC → #f1f4f2 (plus surface-raised added)
    - --color-border: #E2E8F0 → #e0e6ed (plus border-subtle added)
    - 9 new tokens added (text-primary, text-secondary, text-muted, warning, warning-subtle, secondary, error, success)
    - --color-primary kept with legacy comment (CalendarView uses text-primary; migrated in Plan 02)
    - @layer base body: unchanged
  </action>
  <verify>cd app && npm run build — exits 0, no CSS errors in output</verify>
  <done>AC-1 + AC-2 + AC-3 satisfied: 15 tokens present, accent updated, Inter removed, build green</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- app/index.html (Jost already loaded there — do not duplicate)
- @layer base body block (keep exactly as-is)
- Any .tsx / .ts component files (color replacement is Plan 18.2-02)

## SCOPE LIMITS
- This plan is index.css only — one file, pure CSS token definitions
- Do not replace hardcoded colors in components (that is Plan 18.2-02)
- Do not touch AppShell layout (that is Plan 18.2-03)
- --color-primary is intentionally kept (legacy; CalendarView migration deferred to Plan 02)
</boundaries>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm run build` exits 0
- [ ] index.css @theme contains exactly the 15 new tokens plus --color-primary legacy
- [ ] No `Inter` in the @import URL
- [ ] --color-accent value is #3ab9e3 (not #6366f1)
- [ ] @layer base body block is unchanged
</verification>

<success_criteria>
- Task 1 completed
- All verification checks pass
- No component files modified
</success_criteria>

<output>
After completion, create `.paul/phases/18.2-ui-design-system/18.2-01-SUMMARY.md`
</output>
