---
phase: 18.2-ui-design-system
plan: 02
type: summary
status: complete
date: 2026-04-01
---

# Plan 18.2-02 Summary — Token Sweep & Corner Radius

## Outcome
Both tasks completed. All verification checks pass. Build exits 0.

## Tasks Executed

### Task 1: Shared UI components — sharp corners and StatusBadge tokens
**Status: PASS**

- `Button.tsx`: `rounded-xl` → `rounded`
- `Card.tsx`: `rounded-2xl` → `rounded`
- `Input.tsx`: `rounded-xl` → `rounded`
- `Select.tsx`: `rounded-xl` → `rounded`
- `StatusBadge.tsx`: replaced hard-coded Tailwind color pairs with design token classes:
  - `završeno`: `bg-success/10 text-success`
  - `djelomično završeno`: `bg-accent-subtle text-accent`
  - `čeka materijal`: `bg-warning-subtle text-text-secondary`
  - `blokirano`: `bg-error/10 text-error`
  - `potrebno dodatno`: `bg-secondary/10 text-secondary`

### Task 2: Feature file hex sweep + text-primary migration + index.css cleanup
**Status: PASS (with scope extension — see note)**

- `index.css`: removed `--color-primary` legacy token block
- `CalendarView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`; `text-primary` → `text-text-primary`
- `DashboardView.tsx`: `|| '#6366f1'` → `|| 'var(--color-accent)'`
- `DiaryEntryDetailModal.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ReminderOverlay.tsx`: both inline style fallbacks → `'var(--color-accent)'`
- `MasterProjectDetailView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `MasterProjectsListView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `MasterWorkspacePage.tsx`: both `company.brandColor || '#3b82f6'` → `'var(--color-accent)'`
- `CompanySettingsView.tsx`: both `#6366f1` → `#3ab9e3` (color picker — real hex needed)
- `InviteUserModal.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `NewProjectView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ProjectDetailView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ProjectDocumentsTab.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ProjectMembersTab.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ProjectsView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ProjectTasksTab.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`
- `ReportsView.tsx`: `|| '#3b82f6'` → `|| 'var(--color-accent)'`; all `text-primary` → `text-text-primary` (8 occurrences — scope extension, see note)
- `generateDiaryPdf.ts`: `|| '#3b82f6'` → `|| '#3ab9e3'` (hex required for hexToRgb())

**Note — scope extension:** The plan only mentioned CalendarView for `text-primary` migration, but `ReportsView.tsx` had 8 additional usages. Removing `--color-primary` from index.css without migrating ReportsView would have caused a visual regression (text losing its color). Since ReportsView is in `files_modified` and the plan's intent was zero regressions, all 8 occurrences were also migrated to `text-text-primary`.

## Verification Results
- `grep -r "#6366f1\|#3b82f6" app/src --include="*.tsx" --include="*.ts"` → returns only AppShell.tsx (Plan 18.2-03 scope) ✓
- `grep "--color-primary" app/src/index.css` → nothing ✓
- `grep "text-primary" app/src/features/calendar/components/CalendarView.tsx` → nothing (class migrated) ✓
- Button/Card/Input/Select: no `rounded-xl` or `rounded-2xl` ✓
- `npm run build` → exits 0 ✓

## Acceptance Criteria
- [x] AC-1: Shared components use `rounded` (4px) — no `rounded-xl` or `rounded-2xl`
- [x] AC-2: StatusBadge uses design token colors
- [x] AC-3: No old accent hex fallbacks in feature files (AppShell.tsx is Plan 18.2-03 scope)
- [x] AC-4: `text-primary` migrated in CalendarView + ReportsView; `--color-primary` removed
- [x] AC-5: Build passes

## Session Additions (2026-04-01 — out-of-band, pre-applied for Plan 18.2-03)

Additional UI polish applied directly at user request, outside plan scope. Pre-applied so Plan 03 does not re-touch these:

| File | Change |
|------|--------|
| `LandingPage.tsx` | All 5 CTA buttons: `rounded-xl`/`rounded-lg` → `rounded` |
| `ProjectsView.tsx` | Both filter pill rows: `rounded-full` → `rounded` |
| `AppShell.tsx` | Desktop sidebar NavItem: `rounded-xl` → `rounded` |
| `DashboardView.tsx` | Google Calendar connect notification: `rounded-2xl` → `rounded`; removed calendar events block below stat cards (+ cleaned unused imports) |
