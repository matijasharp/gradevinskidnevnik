---
phase: 18.2-ui-design-system
plan: 01
status: complete
completed: 2026-04-01
---

## What Was Done

Replaced the 4-token @theme stub in `app/src/index.css` with the full 15-token design system.

## Changes

**app/src/index.css**
- @import: Removed `Inter` font (Jost already loaded in index.html); kept JetBrains Mono
- `--font-sans`: "Inter" → "Jost"
- `--color-accent`: #6366f1 → #3ab9e3 (all existing `bg-accent`/`text-accent` classes now render the new color)
- `--color-surface`: #F8FAFC → #f1f4f2
- `--color-border`: #E2E8F0 → #e0e6ed
- Added 11 new tokens: `--color-accent-hover`, `--color-accent-subtle`, `--color-text-primary`, `--color-text-secondary`, `--color-text-muted`, `--color-surface-raised`, `--color-border-subtle`, `--color-warning`, `--color-warning-subtle`, `--color-secondary`, `--color-error`, `--color-success`
- `--color-primary: #0F172A` kept as legacy (CalendarView uses `text-primary`; to be migrated in Plan 02)
- `@layer base` body block: unchanged

## Verification

- `npm run build` ✅ exits 0, no CSS errors
- Chunk size warning is pre-existing, not introduced here

## Deferred to Plan 18.2-02

- `--color-primary` removal — after `text-primary` in CalendarView is migrated to `text-text-primary`
- All hardcoded `#6366f1` / `#3b82f6` / `#192a46` in component files
- `rounded-xl`/`rounded-2xl` replacement in shared components
