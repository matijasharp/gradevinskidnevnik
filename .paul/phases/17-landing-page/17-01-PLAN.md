---
phase: 17-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/index.html
  - app/public/favicon.svg
  - app/src/features/landing/LandingPage.tsx
  - app/src/app/router/routeConfig.ts
  - app/src/app/router/AppRouter.tsx
autonomous: true
---

<objective>
## Goal
Build a professional Croatian marketing landing page at `/landing` targeting electricians, matching the app's icon color palette, with proper favicon and OG metadata.

## Purpose
Phase 17 delivers the marketing surface for the Electro MVP launch. The page must convince electricians to try the app with zero fluff — all copy in Croatian, mobile-first, dark-hero aesthetic matching the app's visual identity (icon palette of indigo/blue/cyan gradients on dark slate).

## Output
- `app/public/favicon.svg` — brand favicon
- `app/index.html` — updated head with title, description, OG tags, favicon
- `app/src/features/landing/LandingPage.tsx` — full landing page component (7 sections)
- `app/src/app/router/routeConfig.ts` — LANDING route constant added
- `app/src/app/router/AppRouter.tsx` — public `/landing` route added before wildcard
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Source Files
@app/index.html
@app/src/app/router/AppRouter.tsx
@app/src/app/router/routeConfig.ts
@app/src/app/router/GuestRoute.tsx
@app/src/index.css
</context>

<acceptance_criteria>

## AC-1: Landing Page Accessible Without Auth
```gherkin
Given the user is not logged in
When they navigate to /landing
Then the landing page renders fully (all 7 sections visible)
And no redirect to login occurs
```

## AC-2: Authenticated Users Redirected Away
```gherkin
Given the user is logged in
When they navigate to /landing
Then they are redirected to the dashboard (/)
```

## AC-3: All Copy Is Croatian and Correct
```gherkin
Given the landing page is rendered
When the user reads all section headings and body copy
Then all text matches the specified Croatian copy exactly
And all CTAs link to / (signup/login flow)
```

## AC-4: Visual Design Matches Icon Palette
```gherkin
Given the landing page is rendered
When viewed on mobile (375px) and desktop (1280px)
Then hero uses dark slate background (#0F172A) with indigo-to-cyan gradient headline
And CTAs use blue-500 (#3B82F6) consistent with app accent
And design is visually consistent with the app's icon aesthetic (rich, modern, dark-first)
```

## AC-5: Head Metadata Professional
```gherkin
Given index.html is loaded in any browser
When the page is shared on social media or indexed by search engines
Then title shows "Gradevinski Dnevnik — Digitalni dnevnik za izvodače"
And og:title, og:description, og:image, og:url are populated
And favicon renders as the brand SVG icon
```

## AC-6: Mobile-First Layout
```gherkin
Given the landing page is rendered on a 375px wide screen
When all sections are scrolled through
Then no horizontal overflow exists
And all text is readable (min 14px)
And CTA buttons are touch-friendly (min 44px height)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Favicon and head metadata</name>
  <files>app/public/favicon.svg, app/index.html</files>
  <action>
    1. Create `app/public/` directory.

    2. Copy the real app logo to public:
       Copy `app/src/shared/icons/gradevinski-dnevnik-logo-icon.svg` → `app/public/favicon.svg`
       This is the canonical brand icon. Use the Bash tool: `cp app/src/shared/icons/gradevinski-dnevnik-logo-icon.svg app/public/favicon.svg`

    3. Update `app/index.html`:
       - Set lang="hr" on <html>
       - Title: "Gradevinski Dnevnik — Digitalni dnevnik za izvodače"
       - Add: <meta name="description" content="Sve slike, faze, izvještaji i potpisi na jednom mjestu. Profesionalni dnevnik radova za električare.">
       - Add: <link rel="icon" href="/favicon.svg" type="image/svg+xml">
       - Add OG tags:
         <meta property="og:type" content="website">
         <meta property="og:title" content="Gradevinski Dnevnik — Digitalni dnevnik za izvodače">
         <meta property="og:description" content="Sve slike, faze, izvještaji i potpisi na jednom mjestu. Profesionalni dnevnik radova za električare.">
         <meta property="og:image" content="https://elektro.gradevinskidnevnik.online/favicon.svg">
         <meta property="og:url" content="https://elektro.gradevinskidnevnik.online/landing">
         <meta property="og:site_name" content="Gradevinski Dnevnik">
         <meta property="og:locale" content="hr_HR">
       - Add Twitter card:
         <meta name="twitter:card" content="summary">
         <meta name="twitter:title" content="Gradevinski Dnevnik — Digitalni dnevnik za izvodače">
         <meta name="twitter:description" content="Digitalni dnevnik radova za izvodače. Slike, izvještaji i potpisi na jednom mjestu.">
         <meta name="twitter:image" content="https://elektro.gradevinskidnevnik.online/favicon.svg">
       - Keep existing viewport, charset, and script tags unchanged.
  </action>
  <verify>
    Open app/index.html in browser dev tools — head tags all present. Favicon appears in browser tab. Run: npm run build (no errors).
  </verify>
  <done>AC-5 satisfied: professional head metadata + favicon in place</done>
</task>

<task type="auto">
  <name>Task 2: Build LandingPage component</name>
  <files>app/src/features/landing/LandingPage.tsx</files>
  <action>
    Create `app/src/features/landing/LandingPage.tsx` as a single self-contained React component.
    No external imports needed beyond React and react-router-dom (Link).

    **Design System:**
    - Font: Inter (already loaded via Google Fonts in index.css)
    - Primary dark: #0F172A (slate-900)
    - Surface dark: #1E293B (slate-800)
    - Accent blue: #3B82F6 (blue-500)
    - Accent indigo: #6366F1 (indigo-500)
    - Text on dark: #F1F5F9 (slate-100), muted: #94A3B8 (slate-400)
    - Text on light: #0F172A (slate-900), muted: #475569 (slate-600)
    - Gradient headline: bg-gradient-to-r from-indigo-400 via-blue-400 to-cyan-400 bg-clip-text text-transparent
    - Border accent: border-indigo-500/30
    - Use Tailwind utility classes throughout (already configured in the app)

    **Page structure — implement each section exactly:**

    ---
    ### SECTION 1: Navbar (sticky, minimal)
    - sticky top-0 z-50, bg-slate-900/95 backdrop-blur border-b border-slate-800
    - Left: Text logo "Gradevinski Dnevnik" in white, small lightning bolt emoji ⚡ before it
    - Right: Two buttons:
      - "Zatraži demo" — ghost style (text-slate-300 hover:text-white)
      - "Isprobaj besplatno" — filled blue (bg-blue-500 hover:bg-blue-600 text-white rounded-lg px-4 py-2 text-sm font-medium)
    - Both link to "/" (Link from react-router-dom)
    - Mobile: stack or show only the filled CTA button

    ---
    ### SECTION 2: Hero
    - Full-screen section: min-h-screen bg-slate-900 flex flex-col justify-center
    - Centered content: max-w-4xl mx-auto px-6 text-center
    - Eyebrow pill: small badge "Za električare" — bg-indigo-500/10 text-indigo-400 border border-indigo-500/20 rounded-full px-3 py-1 text-sm mb-6
    - H1: "Digitalni dokaz radova za električare." — text-4xl md:text-6xl font-bold leading-tight
      Apply gradient: bg-gradient-to-r from-indigo-400 via-blue-400 to-cyan-400 bg-clip-text text-transparent
    - Subheadline: "Sve slike, faze, izvještaji, glasovne bilješke i potpisi na jednom mjestu — spremno za klijenta i naplatu." — text-xl md:text-2xl text-slate-300 mt-6 max-w-2xl mx-auto
    - Supporting text: "Ne traži slike po WhatsAppu. Ne objašnjavaj ponovno što je napravljeno. Svaki projekt ima svoje slike, dnevne unose, potpis i PDF izvještaj koji možeš poslati klijentu u nekoliko klikova." — text-slate-400 mt-4 max-w-xl mx-auto text-base leading-relaxed
    - CTA row (mt-10 flex flex-col sm:flex-row gap-4 justify-center):
      - Primary: Link to "/" — "Isprobaj besplatno →" — bg-blue-500 hover:bg-blue-600 text-white font-semibold px-8 py-4 rounded-xl text-lg transition-colors
      - Secondary: Link to "/" — "Zatraži demo" — border border-slate-600 hover:border-slate-400 text-slate-300 hover:text-white px-8 py-4 rounded-xl text-lg transition-colors
    - Subtle bottom gradient fade into next section

    ---
    ### SECTION 3: Problem
    - bg-white, py-20 px-6
    - max-w-3xl mx-auto
    - Section label (small caps): "Problem" — text-blue-500 font-semibold text-sm tracking-widest uppercase mb-4
    - H2: "Koliko puta ti se dogodilo da:" — text-3xl md:text-4xl font-bold text-slate-900 mb-10
    - Problem list — each item as a card row with ❌ icon on left:
      Items (each in a bordered row, bg-slate-50 rounded-xl p-4 mb-3 flex items-start gap-3):
      1. "slike ostanu u galeriji ili WhatsAppu"
      2. "klijent kaže da nešto nije napravljeno"
      3. "ne znaš tko je što radio na projektu"
      4. "trebaš ponovno sastaviti izvještaj za klijenta"
      5. "nemaš dokaz što je napravljeno i kada"
      Icon: text-red-400 text-lg "✗", text: text-slate-700 font-medium
    - Conclusion box (mt-10 bg-slate-900 text-white rounded-2xl p-8 text-center):
      "Umjesto kaosa po mobitelu, sve imaš na jednom mjestu." — text-xl font-semibold

    ---
    ### SECTION 4: How It Works
    - bg-slate-50, py-20 px-6
    - max-w-4xl mx-auto
    - Section label: "Kako radi" — text-blue-500 uppercase tracking-widest text-sm font-semibold mb-4
    - H2: "Tri koraka do urednog projekta" — text-3xl md:text-4xl font-bold text-slate-900 mb-12
    - 3-step grid (grid grid-cols-1 md:grid-cols-3 gap-8):
      Each step card: bg-white rounded-2xl p-8 shadow-sm border border-slate-100 relative
      Step number bubble: absolute top-6 right-6, w-8 h-8 rounded-full bg-blue-500 text-white text-sm font-bold flex items-center justify-center
      Steps:
      1. Number: "1" | Title: "Kreiraj projekt" | Body: "Dodaj klijenta, lokaciju i osnovne podatke o projektu."
      2. Number: "2" | Title: "Dodaj dnevni unos" | Body: "Unesi što je napravljeno, dodaj slike, materijale i potpis. Možeš i snimiti kratku glasovnu poruku s terena, a sustav automatski pretvara govor u profesionalan izvještaj."
      3. Number: "3" | Title: "Pošalji PDF klijentu" | Body: "U nekoliko klikova generiraš profesionalan izvještaj koji možeš poslati klijentu ili spremiti za sebe."
      Title: text-lg font-bold text-slate-900 mb-3
      Body: text-slate-600 text-sm leading-relaxed

    ---
    ### SECTION 5: Features (What You Get)
    - bg-white, py-20 px-6
    - max-w-5xl mx-auto
    - Section label: "Što dobivaš" — text-blue-500 uppercase tracking-widest text-sm font-semibold mb-4
    - H2: "Sve što trebaju izvodači" — text-3xl md:text-4xl font-bold text-slate-900 mb-12
    - 6-feature grid (grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6):
      Each feature card: bg-slate-50 rounded-2xl p-6 border border-slate-100
      Icon area: w-10 h-10 rounded-xl bg-indigo-500/10 flex items-center justify-center mb-4, icon text-indigo-500 text-xl
      Title: text-base font-semibold text-slate-900 mb-2
      Body: text-slate-600 text-sm leading-relaxed

      Features (use emoji as icon):
      1. 🖼️ | "Sve slike na jednom mjestu" | "Više ne tražiš slike po galeriji i WhatsAppu."
      2. 📋 | "Dnevni izvještaji" | "Svaki rad, faza i problem ostaju zapisani. Glasovne poruke automatski se pretvaraju u uredan tekst i izvještaj."
      3. ✅ | "Dokaz izvedenih radova" | "Ako klijent kaže da nešto nije napravljeno, imaš slike, datum, potpis i zapis što je napravljeno."
      4. 💳 | "Lakša naplata" | "Kad završiš posao, imaš gotov PDF koji možeš poslati klijentu."
      5. 👥 | "Tim i suradnja" | "Vidi tko je radio na projektu i što je napravljeno."
      6. ⚡ | "Napravljeno za električare" | "Specifično za električne instalacije i izvodače koji rade na terenu."

    ---
    ### SECTION 6: Social Proof
    - bg-slate-900, py-20 px-6
    - max-w-4xl mx-auto text-center
    - Section label: "Društveni dokaz" text-indigo-400 uppercase tracking-widest text-sm font-semibold mb-4
    - H2: "Napravljen za električare koji žele manje kaosa i više kontrole." — text-3xl md:text-4xl font-bold text-white mb-12
    - 3-testimonial grid (grid grid-cols-1 md:grid-cols-3 gap-6):
      Each card: bg-slate-800 rounded-2xl p-6 border border-slate-700 text-left
      Quote mark: text-indigo-400 text-4xl font-serif leading-none mb-3 (use " character)
      Quote text: text-slate-200 text-sm leading-relaxed italic mb-4
      Attribution: text-slate-500 text-xs (use dash + "Električni izvodač")

      Testimonials:
      1. "Napokon imam sve slike i izvještaje na jednom mjestu." — Električni izvodač
      2. "Više ne moram klijentu tražiti stare slike po WhatsAppu." — Električni izvodač
      3. "Najviše mi znači što mogu odmah poslati PDF nakon obilaska." — Električni izvodač

    ---
    ### SECTION 7: Pilot Offer
    - bg-gradient-to-br from-indigo-950 via-slate-900 to-slate-900, py-20 px-6
    - max-w-3xl mx-auto text-center
    - Section label: text-indigo-400 uppercase tracking-widest text-sm font-semibold mb-4 "Osnivačka ponuda"
    - H2: "Tražimo prvih 10 električara." — text-4xl font-bold text-white mb-6
    - Body: "Prvih 10 firmi dobiva:" — text-slate-300 text-lg mb-8
    - 4-benefit list (inline centered, space-y-3):
      Each item: flex items-center gap-3 text-slate-200
      Icon: ✓ in bg-green-500/20 text-green-400 rounded-full w-6 h-6 flex items-center justify-center text-sm
      Items:
      - "60 dana besplatno"
      - "Founder cijenu zauvijek"
      - "Pomoć kod onboardinga"
      - "Mogućnost utjecaja na razvoj proizvoda"
    - Pricing row (mt-12 grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-xl mx-auto):
      Each pricing card: bg-slate-800/50 border border-indigo-500/30 rounded-2xl p-6 text-center
      Card 1: Plan name "Izvođač" text-slate-300, Price "19 €/mj" text-3xl font-bold text-white, Sub "zauvijek"  text-indigo-400 text-sm
      Card 2: Plan name "Izvođač Pro" text-slate-300, Price "49 €/mj" text-3xl font-bold text-white, Sub "zauvijek" text-indigo-400 text-sm — add a "Popular" badge: absolute top-3 right-3 bg-indigo-500 text-white text-xs px-2 py-1 rounded-full
    - CTA (mt-12): Link to "/" — "Postani jedan od prvih 10 →" — bg-blue-500 hover:bg-blue-600 text-white font-semibold px-10 py-4 rounded-xl text-lg inline-block

    ---
    ### SECTION 8: Referral
    - bg-white, py-16 px-6
    - max-w-2xl mx-auto text-center
    - H2: "Preporuči kolegu" — text-2xl font-bold text-slate-900 mb-4
    - Body: "Ako preporučiš drugu firmu koja krene koristiti aplikaciju, dobivaš mjesec dana besplatno. Novi korisnik dobiva 50% popusta prvi mjesec." — text-slate-600 leading-relaxed mb-8
    - Two-column benefit callout (bg-slate-50 rounded-2xl p-6 grid grid-cols-2 gap-4 text-center):
      Left: "Ti dobivaš" label text-slate-500 text-sm mb-1, "1 mjesec besplatno" text-lg font-bold text-slate-900
      Right: "Novi korisnik dobiva" label text-slate-500 text-sm mb-1, "50% popusta" text-lg font-bold text-indigo-500 "prvi mjesec" text-sm text-slate-500

    ---
    ### SECTION 9: Final CTA + Footer
    - bg-slate-900, py-16 px-6 text-center
    - H2: "Spreman isprobati?" — text-3xl font-bold text-white mb-4
    - Body: "Pridruži se prvim korisnicima i dobij founder cijenu zauvijek." — text-slate-400 mb-8
    - CTA: Link to "/" — "Isprobaj besplatno →" — same primary button style as hero
    - Footer below (border-t border-slate-800 mt-16 pt-8):
      Text: "© 2025 Gradevinski Dnevnik. Sve slike, faze, izvještaji i potpisi." text-slate-600 text-sm

    **Implementation notes:**
    - Use `Link` from `react-router-dom` for all CTA href="#" type links — point to `"/"`
    - All sections are stacked vertically in one component — no lazy loading
    - Use `scroll-smooth` on the html element or just let browser default
    - Avoid any imports from the app feature modules — LandingPage must be self-contained
    - No TypeScript interfaces needed beyond props (component has no props)
    - Do NOT import from shared/ui — write inline Tailwind classes directly (keeps component isolated)
  </action>
  <verify>
    Run: cd app && npm run build — zero TypeScript errors.
    Navigate to /landing in dev server — all 9 sections render.
    Chrome DevTools: check mobile (375px) — no overflow, all sections readable.
  </verify>
  <done>AC-1, AC-3, AC-4, AC-6 satisfied: landing page renders all sections with correct Croatian copy, icon-matching colors, and mobile-first layout</done>
</task>

<task type="auto">
  <name>Task 3: Wire public /landing route in router</name>
  <files>app/src/app/router/routeConfig.ts, app/src/app/router/AppRouter.tsx</files>
  <action>
    1. In `app/src/app/router/routeConfig.ts`:
       Add to the ROUTES object:
       ```
       LANDING: '/landing',
       ```

    2. In `app/src/app/router/AppRouter.tsx`:
       - Import LandingPage: `import LandingPage from '../../features/landing/LandingPage';`
       - Import GuestRoute: `import GuestRoute from './GuestRoute';`
       - Add an explicit route for '/landing' BEFORE the '*' wildcard in the router array:
         ```
         {
           path: '/landing',
           element: <GuestRoute><LandingPage /></GuestRoute>,
         },
         ```
       - Keep the existing `{ path: '*', element: <OrganizationProvider><App /></OrganizationProvider> }` unchanged.

    This means:
    - Unauthenticated: /landing → LandingPage (GuestRoute passes through)
    - Authenticated: /landing → GuestRoute redirects to / (dashboard)
    - All other routes: handled by existing App wildcard (unchanged behavior)
  </action>
  <verify>
    Run: npm run build — no errors.
    Dev server: unauthenticated browser → /landing shows landing page.
    Dev server: authenticated browser → /landing redirects to /.
  </verify>
  <done>AC-1, AC-2 satisfied: /landing route accessible to guests, redirects authenticated users</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- app/src/App.tsx (auth flow, routing logic, all app routes)
- app/src/app/router/ProtectedRoute.tsx
- app/src/app/providers/ (AuthProvider, OrganizationProvider)
- app/src/index.css (design tokens)
- All existing ROUTES values in routeConfig.ts (only ADD LANDING, don't modify others)
- Any existing database/Supabase code
- app/.env and app/.env.example

## SCOPE LIMITS
- No images/raster assets — SVG and emoji only for this phase
- No animations library (Motion/Framer) — CSS transitions only (transition-colors, etc.)
- No real testimonials or customer data — use the placeholder text provided
- LandingPage must NOT import from app features (self-contained)
- Do not change the root `/` route behavior — it still handles auth/app as before
- og:image uses the real logo SVG at the production domain — raster OG image is a post-MVP concern
- Do NOT change the production domain string (elektro.gradevinskidnevnik.online)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run build passes with zero errors
- [ ] /landing renders in browser without login (test in incognito)
- [ ] All 9 sections visible: Navbar, Hero, Problem, How It Works, Features, Social Proof, Pilot Offer, Referral, Footer
- [ ] Croatian copy matches spec exactly (headline: "Digitalni dokaz radova za električare.")
- [ ] Mobile 375px: no horizontal scroll, all CTAs tappable
- [ ] index.html has correct title, OG tags, and favicon link
- [ ] favicon.svg renders in browser tab
- [ ] Authenticated user at /landing → redirected to /
- [ ] All existing app routes still work (/projects, /dashboard, etc.)
</verification>

<success_criteria>
- All tasks completed
- npm run build passes
- Landing page accessible at /landing without auth
- All verification checks pass
- No regressions in existing app routes
</success_criteria>

<output>
After completion, create `.paul/phases/17-landing-page/17-01-SUMMARY.md`
</output>
