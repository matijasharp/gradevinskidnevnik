---
phase: 01-contractor-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/.env.example
  - app/src/lib/supabase.ts
  - app/src/lib/data.ts
  - app/src/App.tsx
  - supabase/migrations/20260327_01_init.sql
  - supabase/policies/20260327_01_rls.sql
autonomous: true
---

<objective>
## Goal
Establish Supabase foundation (client + schema + RLS) and wire core read paths in the main app while preserving prototype UI and mobile design.

## Purpose
Phase 1 requires a Supabase-first backend with RLS and a working app shell before we port full CRUD and advanced features.

## Output
- Supabase client and environment config in the app
- Supabase schema and RLS policies for core entities
- Read-only data access layer connected to the UI
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@projects/gradevinski-dnevnik/PLANNING.md

## Source Files
@app/src/App.tsx
@app/src/lib/firebase.ts
@docs/context-seed/03-data-model-and-permissions.md
@docs/context-seed/04-firestore-current-and-refactor.md
@docs/context-seed/05-supabase-schema.md
</context>

<acceptance_criteria>

## AC-1: Supabase client wired
```gherkin
Given VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are configured
When the app starts
Then the Supabase client initializes without runtime errors and the app builds
```

## AC-2: Core schema and RLS defined
```gherkin
Given the Supabase SQL migration files exist
When the schema is reviewed against context-seed requirements
Then organizations, membership, projects, phases, diary entries, photos, and clients are defined with RLS policies that enforce org/project access
```

## AC-3: Core read paths use Supabase
```gherkin
Given seeded data exists in Supabase
When a user opens the main app
Then project and diary lists render from Supabase while the prototype UI and mobile layout remain unchanged
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add Supabase client and env scaffolding</name>
  <files>app/package.json, app/.env.example, app/src/lib/supabase.ts</files>
  <action>
    - Add @supabase/supabase-js dependency to app/package.json.
    - Create app/src/lib/supabase.ts with a typed Supabase client using import.meta.env.
    - Add app/.env.example documenting VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY (keep existing env keys untouched).
    - Do NOT remove firebase.ts; keep it as reference for parity.
  </action>
  <verify>npm run lint</verify>
  <done>AC-1 satisfied: Supabase client config compiles and initializes cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Define Supabase schema and RLS for core entities</name>
  <files>supabase/migrations/20260327_01_init.sql, supabase/policies/20260327_01_rls.sql</files>
  <action>
    - Create a base schema for organizations, organization_members, clients, projects, project_phases, diary_entries, diary_photos, and user profiles.
    - Include fields required by context-seed (ownerCompanyId, projectType, visibility, entry_number, pdf_url, client_visible, signedByName/Role, workDateFrom/To).
    - Add indexes for project and entry queries (project_id, phase_id, created_at).
    - Define RLS policies that allow:
      - organization members to read/write org-owned projects
      - invited project members to read/write within that project scope
    - Ensure policies support contractor-owned projects and invited master project participation.
  </action>
  <verify>Review SQL files for required tables, columns, and RLS coverage</verify>
  <done>AC-2 satisfied: Core schema and RLS policies align with context-seed requirements</done>
</task>

<task type="auto">
  <name>Task 3: Wire core read paths in the app to Supabase</name>
  <files>app/src/lib/data.ts, app/src/App.tsx</files>
  <action>
    - Create a data access module that fetches organizations, projects, phases, diary entries, and photos from Supabase.
    - Replace Firebase read calls in App.tsx with the new Supabase data module.
    - Keep all UI markup, layout, and styling intact; only swap data sources and add minimal loading/error handling.
    - Ensure empty states do not change the prototype layout or spacing.
  </action>
  <verify>npm run dev and confirm project/diary lists render from Supabase without UI regressions</verify>
  <done>AC-3 satisfied: UI parity preserved while data loads from Supabase</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- app/src/index.css visual styling (layout and typography)
- Overall UI structure in app/src/App.tsx (no redesign)
- app/server.ts OAuth flow (no changes in this plan)

## SCOPE LIMITS
- No new features beyond data wiring (CRUD and advanced features are in later plans)
- No master workspace or subdomain UX in this plan
- No removal of Firebase files yet

</boundaries>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] Manual UI check confirms prototype mobile layout unchanged
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- Supabase foundation is in place without UI regressions
- Core read paths use Supabase data
- No scope violations
</success_criteria>

<output>
After completion, create `.paul/phases/01-contractor-mvp/01-01-SUMMARY.md`
</output>
