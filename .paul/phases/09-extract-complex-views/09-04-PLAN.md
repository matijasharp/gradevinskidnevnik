---
phase: 09-extract-complex-views
plan: 04
type: execute
wave: 1
depends_on: ["09-03"]
files_modified:
  - app/src/features/diary/components/NewEntryView.tsx
  - app/src/App.tsx
autonomous: true
---

<objective>
## Goal
Extract NewEntryView (and its companion constant SUGGESTED_MATERIALS) from App.tsx into `features/diary/components/NewEntryView.tsx` — the multi-step diary entry creation and editing form.

## Purpose
NewEntryView is the last complex component in App.tsx. Extracting it completes Phase 9 and clears the path to Phase 10 (Router Scaffolding). It removes ~810 lines from App.tsx (SUGGESTED_MATERIALS + NewEntryView function), bringing App.tsx close to containing only the root App component and AppContent.

## Output
- `app/src/features/diary/components/NewEntryView.tsx` (created, ~810 lines)
- `app/src/App.tsx` (modified — 1 import added, SUGGESTED_MATERIALS const + inline definition removed, ~−810 lines)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-extract-complex-views/09-03-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: NewEntryView extracted
```gherkin
Given App.tsx contains SUGGESTED_MATERIALS (lines ~1146–1160) and NewEntryView defined inline (lines ~1162–1958, ~796 lines)
When the extract-and-import pattern is applied
Then NewEntryView exists in features/diary/components/NewEntryView.tsx,
     App.tsx imports it from that path,
     the inline definition and SUGGESTED_MATERIALS are removed from App.tsx,
     and npm run build exits 0
```

## AC-2: SUGGESTED_MATERIALS moves with the component
```gherkin
Given SUGGESTED_MATERIALS is defined immediately before NewEntryView in App.tsx and used only by NewEntryView
When the new file is created
Then SUGGESTED_MATERIALS is declared in NewEntryView.tsx (above the function),
     and is no longer present in App.tsx
```

## AC-3: Build integrity maintained
```gherkin
Given the governing constraint — nothing gets broken
When each extraction step completes
Then npm run build exits 0 after: (a) file created + import added, (b) inline + SUGGESTED_MATERIALS removed
     (2 build checks total)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract NewEntryView</name>
  <files>app/src/features/diary/components/NewEntryView.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/diary/components/NewEntryView.tsx`.
    Copy SUGGESTED_MATERIALS const verbatim from App.tsx (lines ~1146–1160), then copy NewEntryView function body verbatim (lines ~1162–1958).
    Add imports at the top of the new file:
    - `import React, { useState, useEffect, useRef } from 'react';`
    - `import { motion } from 'motion/react';`
    - `import { X, Plus, Camera, Mic, Folder, CloudSun, Bell, Zap, Loader2, Trash2 } from 'lucide-react';`
    - `import Markdown from 'react-markdown';`
    - `import SignatureCanvas from 'react-signature-canvas';`
    - `import { useDropzone } from 'react-dropzone';`
    - `import { format } from 'date-fns';`
    - `import { hr } from 'date-fns/locale';`
    - `import { GoogleGenAI, Type } from '@google/genai';`
    - `import { fetchDiaryPhotos, createDiaryEntry, updateDiaryEntry, createDiaryPhoto, uploadDiaryPhoto, updateDiaryPhoto, deleteDiaryPhoto } from '../../../lib/data';`
    - `import type { DiaryEntry, DiaryPhoto, Project } from '../../../shared/types';`
    - `import { compressImage, trimCanvas } from '../../../shared/utils/image';`
    - `import { OperationType, handleFirestoreError } from '../../../shared/utils/error';`
    - `import { cn } from '../../../lib/utils';`
    - `import { Button, Card, Input, Select } from '../../../shared/ui';`
    - `import { getPhases, getWorkTypes } from '../../../lib/disciplineConfig';`
    Export as `export default function NewEntryView`.

    **Step 2 — Add import + build check:**
    Add to App.tsx imports (after the ProjectDetailView import line):
    `import NewEntryView from './features/diary/components/NewEntryView';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition + SUGGESTED_MATERIALS + build check:**
    Delete SUGGESTED_MATERIALS const and NewEntryView function body from App.tsx (lines ~1146–1958, including blank line before SUGGESTED_MATERIALS).
    After removal, App.tsx should end at its last closing `}` with nothing after it — verify that the file no longer defines NewEntryView or SUGGESTED_MATERIALS inline.
    Run `npm run build` — must exit 0.
  </action>
  <verify>
    - `grep -n "^function NewEntryView" app/src/App.tsx` → 0 results
    - `grep -n "^const SUGGESTED_MATERIALS" app/src/App.tsx` → 0 results
    - `grep -n "NewEntryView" app/src/App.tsx` → only the import line + 2 call sites (~line 763 and ~775)
    - `grep -n "SUGGESTED_MATERIALS" app/src/features/diary/components/NewEntryView.tsx` → present
    - `npm run build` exits 0
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: NewEntryView in features/diary/components/NewEntryView.tsx, SUGGESTED_MATERIALS moved with it, imported in App.tsx, inline removed, build green</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `function App()` and `function AppContent` in App.tsx — not in scope
- All extracted features in `features/` — no modifications to already-extracted components
- All files in `supabase/migrations/` — locked
- Existing behavior of NewEntryView — no logic changes, verbatim extraction
- The two call sites of NewEntryView in App.tsx (~lines 763, 775) — already use the component correctly; do not touch

## SCOPE LIMITS
- No typing improvements — all `any` stays (Phase 10)
- No Lucide icon pruning in App.tsx imports — deferred until all views extracted
- No restructuring of component internals — copy verbatim
- SUGGESTED_MATERIALS moves to the new file; do not leave it in App.tsx

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "^function NewEntryView" app/src/App.tsx` → 0 results
- [ ] `grep -n "^const SUGGESTED_MATERIALS" app/src/App.tsx` → 0 results
- [ ] `app/src/features/diary/components/NewEntryView.tsx` exists
- [ ] `grep -n "SUGGESTED_MATERIALS" app/src/features/diary/components/NewEntryView.tsx` → present
- [ ] `npm run build` exits 0 (final build)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- NewEntryView extracted and imported
- SUGGESTED_MATERIALS moved to NewEntryView.tsx (not in App.tsx)
- `npm run build` passes throughout (2 build checks)
- No behavior changes — verbatim extraction
- App.tsx reduced by ~810 lines
- Phase 9 complete (all 4 plans done)
</success_criteria>

<output>
After completion, create `.paul/phases/09-extract-complex-views/09-04-SUMMARY.md`
</output>
