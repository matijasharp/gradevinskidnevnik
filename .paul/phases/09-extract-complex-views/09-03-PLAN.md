---
phase: 09-extract-complex-views
plan: 03
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - app/src/features/projects/components/ProjectDetailView.tsx
  - app/src/App.tsx
autonomous: true
---

<objective>
## Goal
Extract ProjectDetailView from App.tsx into `features/projects/components/ProjectDetailView.tsx` — the main project workspace component that hosts the tab navigation (Dnevnik, Suradnici, Zadaci, Dokumenti) and the diary entry timeline.

## Purpose
ProjectDetailView is the last complex component in the projects feature. Extracting it clears the path for Plan 09-04 (NewEntryView) and reduces App.tsx by ~450 lines. All three tab components it depends on (ProjectMembersTab, ProjectTasksTab, ProjectDocumentsTab) are already extracted — they become relative sibling imports.

## Output
- `app/src/features/projects/components/ProjectDetailView.tsx` (created, ~450 lines)
- `app/src/App.tsx` (modified — 1 import added, inline definition removed, ~−450 lines)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-extract-complex-views/09-02-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: ProjectDetailView extracted
```gherkin
Given App.tsx contains ProjectDetailView defined inline (lines ~1145–1593, ~450 lines)
When the extract-and-import pattern is applied
Then ProjectDetailView exists in features/projects/components/ProjectDetailView.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-2: Tab component imports are relative siblings
```gherkin
Given ProjectDetailView uses ProjectMembersTab, ProjectTasksTab, ProjectDocumentsTab
When the new file is created
Then it imports all three from './ProjectMembersTab', './ProjectTasksTab', './ProjectDocumentsTab'
     (relative sibling imports, not from App.tsx)
```

## AC-3: Build integrity maintained
```gherkin
Given the governing constraint — nothing gets broken
When each extraction step completes
Then npm run build exits 0 after: (a) file created + import added, (b) inline removed
     (2 build checks total)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract ProjectDetailView</name>
  <files>app/src/features/projects/components/ProjectDetailView.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/projects/components/ProjectDetailView.tsx`.
    Copy the ProjectDetailView function body verbatim from App.tsx (lines ~1145–1593).
    Add imports at the top of the new file:
    - `import React, { useState, useEffect } from 'react';`
    - `import { motion } from 'motion/react';`
    - `import { X, Trash2, CheckCircle2, Download, Plus, Loader2, User as UserIcon, MapPin, History, Clock, Search, Calendar, AlertTriangle } from 'lucide-react';`
    - `import Markdown from 'react-markdown';`
    - `import { fetchProjectPhotos } from '../../../lib/data';`
    - `import type { DiaryEntry, DiaryPhoto, Project } from '../../../shared/types';`
    - `import { safeFormatDate } from '../../../shared/utils/format';`
    - `import { OperationType, handleFirestoreError } from '../../../shared/utils/error';`
    - `import { cn } from '../../../lib/utils';`
    - `import { Button, Card, StatusBadge } from '../../../shared/ui';`
    - `import { getPhases } from '../../../lib/disciplineConfig';`
    - `import PhotoGallery from '../../diary/components/PhotoGallery';`
    - `import ProjectMembersTab from './ProjectMembersTab';`
    - `import ProjectTasksTab from './ProjectTasksTab';`
    - `import ProjectDocumentsTab from './ProjectDocumentsTab';`
    Export as `export default function ProjectDetailView`.

    **Step 2 — Add import + build check:**
    Add to App.tsx imports (after the DiaryEntryDetailModal import line, with the other project component imports):
    `import ProjectDetailView from './features/projects/components/ProjectDetailView';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition + build check:**
    Delete the ProjectDetailView function body from App.tsx (lines ~1145–1593, including the surrounding blank line before it).
    The line immediately after the removed block is `const SUGGESTED_MATERIALS` — do NOT touch it.
    Run `npm run build` — must exit 0.
  </action>
  <verify>
    - `grep -n "^function ProjectDetailView" app/src/App.tsx` → 0 results
    - `grep -n "ProjectDetailView" app/src/App.tsx` → only the import line + call site (line ~742)
    - `grep -n "SUGGESTED_MATERIALS" app/src/App.tsx` → still present
    - `npm run build` exits 0
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied: ProjectDetailView in features/projects/components/ProjectDetailView.tsx, imported, inline removed, tab component imports are relative siblings, build green</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `const SUGGESTED_MATERIALS` block in App.tsx — belongs to NewEntryView, Plan 09-04 scope
- `function NewEntryView` — Plan 09-04 scope
- All files in `supabase/migrations/` — locked
- Existing behavior of ProjectDetailView — no logic changes, verbatim extraction
- The three already-extracted tab components (ProjectMembersTab, ProjectTasksTab, ProjectDocumentsTab)

## SCOPE LIMITS
- No typing improvements — all `any` stays (Phase 10)
- No Lucide icon pruning in App.tsx imports — deferred until all views extracted
- No restructuring of component internals — copy verbatim
- Do not extract NewEntryView — that is Plan 09-04 scope

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "^function ProjectDetailView" app/src/App.tsx` → 0 results
- [ ] `app/src/features/projects/components/ProjectDetailView.tsx` exists
- [ ] `grep -n "SUGGESTED_MATERIALS" app/src/App.tsx` → still present
- [ ] `npm run build` exits 0 (final build)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- ProjectDetailView extracted and imported
- Tab component imports are relative siblings (./ProjectMembersTab etc.)
- `npm run build` passes throughout (2 build checks)
- No behavior changes — verbatim extraction
- App.tsx reduced by ~450 lines
- SUGGESTED_MATERIALS and NewEntryView untouched
</success_criteria>

<output>
After completion, create `.paul/phases/09-extract-complex-views/09-03-SUMMARY.md`
</output>
