---
phase: 09-extract-complex-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/features/projects/components/ProjectMembersTab.tsx
  - app/src/features/projects/components/ProjectTasksTab.tsx
  - app/src/features/projects/components/ProjectDocumentsTab.tsx
  - app/src/App.tsx
autonomous: true
---

<objective>
## Goal
Extract ProjectMembersTab, ProjectTasksTab, and ProjectDocumentsTab from App.tsx into `features/projects/components/`. These are the three data-fetching tab components that live inside ProjectDetailView.

## Purpose
Each tab has its own async data load (fetchProjectMembers, fetchProjectTasks, fetchProjectDocuments). Extracting them unblocks Plan 09-03 (ProjectDetailView), which renders all three tabs inline. All three go to the same directory and follow the same extract-and-import pattern established in Plan 09-01.

## Output
- `app/src/features/projects/components/ProjectMembersTab.tsx` (created, ~255 lines)
- `app/src/features/projects/components/ProjectTasksTab.tsx` (created, ~145 lines)
- `app/src/features/projects/components/ProjectDocumentsTab.tsx` (created, ~145 lines)
- `app/src/App.tsx` (modified — 3 imports added, 3 inline definitions removed, ~−548 lines)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/09-extract-complex-views/09-01-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: ProjectMembersTab extracted
```gherkin
Given App.tsx contains ProjectMembersTab defined inline (lines ~1592–1846, ~255 lines)
When the extract-and-import pattern is applied
Then ProjectMembersTab exists in features/projects/components/ProjectMembersTab.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-2: ProjectTasksTab extracted
```gherkin
Given App.tsx contains ProjectTasksTab defined inline (lines ~1848–1992, ~145 lines)
When the extract-and-import pattern is applied
Then ProjectTasksTab exists in features/projects/components/ProjectTasksTab.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-3: ProjectDocumentsTab extracted
```gherkin
Given App.tsx contains ProjectDocumentsTab defined inline (lines ~1994–2138, ~145 lines)
When the extract-and-import pattern is applied
Then ProjectDocumentsTab exists in features/projects/components/ProjectDocumentsTab.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-4: Build integrity maintained
```gherkin
Given the governing constraint — nothing gets broken
When each extraction step completes
Then npm run build exits 0 after each of: (a) file created + import added, (b) inline removed
     for all three components (6 build checks total)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract ProjectMembersTab</name>
  <files>app/src/features/projects/components/ProjectMembersTab.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/projects/components/ProjectMembersTab.tsx`.
    Copy the ProjectMembersTab function body verbatim from App.tsx (lines ~1592–1846).
    Add imports at the top of the new file:
    - `import React, { useState, useEffect } from 'react';`
    - `import { Plus, Loader2, Trash2, X, User as UserIcon, Mail, UserPlus } from 'lucide-react';`
    - `import { fetchProjectMembers, fetchProjectInvitations, addProjectMember, createProjectInvitation, removeProjectMember, updateProjectMemberRole, cancelProjectInvitation } from '../../../lib/data';`
    - `import type { ProjectMember, ProjectInvitation } from '../../../lib/data';`
    - `import type { Project } from '../../../shared/types';`
    - `import { Button, Card } from '../../../shared/ui';`
    Export as `export default function ProjectMembersTab`.

    **Step 2 — Add import + build check:**
    Add to App.tsx imports (after the DiaryEntryDetailModal import line):
    `import ProjectMembersTab from './features/projects/components/ProjectMembersTab';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition + build check:**
    Delete the ProjectMembersTab function body from App.tsx (lines ~1592–1846, including surrounding blank lines).
    The next function after it is `ProjectTasksTab` — do NOT touch it.
    Run `npm run build` — must exit 0.
  </action>
  <verify>
    - `grep -n "^function ProjectMembersTab" app/src/App.tsx` → 0 results
    - `grep -n "ProjectMembersTab" app/src/App.tsx` → only the import line + call site(s)
    - `npm run build` exits 0
  </verify>
  <done>AC-1 satisfied: ProjectMembersTab in features/projects/components/ProjectMembersTab.tsx, imported, inline removed, build green</done>
</task>

<task type="auto">
  <name>Task 2: Extract ProjectTasksTab</name>
  <files>app/src/features/projects/components/ProjectTasksTab.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/projects/components/ProjectTasksTab.tsx`.
    Copy the ProjectTasksTab function body verbatim from App.tsx (lines ~1848–1992, adjusting for any shift from Task 1).
    Add imports at the top of the new file:
    - `import React, { useState, useEffect } from 'react';`
    - `import { Plus, Loader2, Trash2 } from 'lucide-react';`
    - `import { fetchProjectTasks, toggleProjectTask, deleteProjectTask, createProjectTask } from '../../../lib/data';`
    - `import type { ProjectTask } from '../../../lib/data';`
    - `import type { Project } from '../../../shared/types';`
    - `import { Button, Card } from '../../../shared/ui';`
    - `import { cn } from '../../../lib/utils';`
    Export as `export default function ProjectTasksTab`.

    **Step 2 — Add import + build check:**
    Add to App.tsx imports (after the ProjectMembersTab import line):
    `import ProjectTasksTab from './features/projects/components/ProjectTasksTab';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition + build check:**
    Delete the ProjectTasksTab function body from App.tsx (including surrounding blank lines).
    The next function after it is `ProjectDocumentsTab` — do NOT touch it.
    Run `npm run build` — must exit 0.
  </action>
  <verify>
    - `grep -n "^function ProjectTasksTab" app/src/App.tsx` → 0 results
    - `grep -n "ProjectTasksTab" app/src/App.tsx` → only the import line + call site(s)
    - `npm run build` exits 0
  </verify>
  <done>AC-2 satisfied: ProjectTasksTab in features/projects/components/ProjectTasksTab.tsx, imported, inline removed, build green</done>
</task>

<task type="auto">
  <name>Task 3: Extract ProjectDocumentsTab</name>
  <files>app/src/features/projects/components/ProjectDocumentsTab.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/projects/components/ProjectDocumentsTab.tsx`.
    Copy the ProjectDocumentsTab function body verbatim from App.tsx (lines ~1994–2138, adjusting for any shift from Tasks 1–2).
    Add imports at the top of the new file:
    - `import React, { useState, useEffect, useRef } from 'react';`
    - `import { Search, Download, Trash2, Plus, FileText, Loader2 } from 'lucide-react';`
    - `import { fetchProjectDocuments, uploadProjectDocument, deleteProjectDocument } from '../../../lib/data';`
    - `import type { ProjectDocument } from '../../../lib/data';`
    - `import type { Project } from '../../../shared/types';`
    - `import { Button, Card } from '../../../shared/ui';`
    Export as `export default function ProjectDocumentsTab`.

    **Step 2 — Add import + build check:**
    Add to App.tsx imports (after the ProjectTasksTab import line):
    `import ProjectDocumentsTab from './features/projects/components/ProjectDocumentsTab';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition + build check:**
    Delete the ProjectDocumentsTab function body from App.tsx (including surrounding blank lines).
    The `const SUGGESTED_MATERIALS` block that follows must remain untouched.
    Run `npm run build` — must exit 0.
  </action>
  <verify>
    - `grep -n "^function ProjectDocumentsTab" app/src/App.tsx` → 0 results
    - `grep -n "ProjectDocumentsTab" app/src/App.tsx` → only the import line + call site(s)
    - `grep -n "SUGGESTED_MATERIALS" app/src/App.tsx` → still present
    - `npm run build` exits 0
  </verify>
  <done>AC-3 and AC-4 satisfied: ProjectDocumentsTab in features/projects/components/ProjectDocumentsTab.tsx, imported, inline removed, SUGGESTED_MATERIALS untouched, build green</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `const SUGGESTED_MATERIALS` block in App.tsx — belongs to NewEntryView, extracted in Plan 09-04
- `ProjectDetailView` inline definition — Plan 09-03 scope
- `NewEntryView` — Plan 09-04 scope
- `DiaryEntryDetailModal`, `PhotoGallery` — already extracted in Plan 09-01
- All files in `supabase/migrations/` — locked
- Existing behavior of all components — no logic changes, verbatim extraction

## SCOPE LIMITS
- No typing improvements — all `any` stays (Phase 10)
- No Lucide icon pruning in App.tsx imports — deferred until all views extracted
- No restructuring of component internals — copy verbatim
- Do not extract `ProjectDetailView` — that is Plan 09-03 scope

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "^function ProjectMembersTab\|^function ProjectTasksTab\|^function ProjectDocumentsTab" app/src/App.tsx` → 0 results
- [ ] All three new files exist in `features/projects/components/`
- [ ] `grep -n "SUGGESTED_MATERIALS" app/src/App.tsx` → still present
- [ ] `npm run build` exits 0 (final build)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- ProjectMembersTab, ProjectTasksTab, ProjectDocumentsTab extracted and imported
- `npm run build` passes throughout (6 intermediate + 1 final build checks)
- No behavior changes — verbatim extraction
- App.tsx reduced by ~548 lines
- SUGGESTED_MATERIALS and ProjectDetailView untouched
</success_criteria>

<output>
After completion, create `.paul/phases/09-extract-complex-views/09-02-SUMMARY.md`
</output>
