---
phase: 18.1-brand-identity
plan: 02
subsystem: ui
tags: [gsap, animation, splash-screen, branding, landing-page, css]

requires:
  - phase: 18.1-brand-identity plan 01
    provides: Jost font, GDO SVG icons, logo.svg at /brand/logo.svg, landing page routing

provides:
  - GSAP splash screen on auth loading (SplashScreen.tsx)
  - Landing page CTA buttons fixed (ROUTES.REPORTS, no redirect loop)
  - Landing navbar logo replaced with SVG
  - Default brand color #6366f1 (indigo-500) throughout app
  - Reset-to-default button in company settings
  - Letter spacing applied globally (0.015em) and on nav labels (tracking-wide)

affects: [19-reports-analytics, any phase touching auth loading or brand color]

tech-stack:
  added: [gsap ^3.14.2]
  patterns: [GSAP timeline in useEffect with ref targets, splashDone state gate on auth loading]

key-files:
  created: [app/src/features/auth/components/SplashScreen.tsx]
  modified:
    - app/package.json
    - app/src/features/auth/components/LoginView.tsx
    - app/src/features/landing/LandingPage.tsx
    - app/src/features/organizations/components/CompanySettingsView.tsx
    - app/src/app/layouts/AppShell.tsx
    - app/src/index.css

key-decisions:
  - "SplashScreen mounted only in LoginView loading check — no per-page animation"
  - "All landing CTAs point to ROUTES.REPORTS (catchall → App → LoginView) not /login"
  - "Default color #6366f1 updated only in AppShell + CompanySettings — other components deferred (out of scope)"

patterns-established:
  - "GSAP animation: fromTo on refs inside useEffect, kill() in cleanup"
  - "splashDone state gates render after animation completes via onComplete callback"

duration: ~45min
started: 2026-03-31T00:00:00Z
completed: 2026-03-31T00:00:00Z
---

# Phase 18.1 Plan 02: Brand Identity Polish Summary

**GSAP splash screen, landing CTA fix, SVG logo, GDO indigo default, reset button, letter spacing — all brand polish deliverables shipped.**

## Performance

| Metric | Value |
|--------|-------|
| Duration | ~45 min |
| Started | 2026-03-31 |
| Completed | 2026-03-31 |
| Tasks | 6 completed |
| Files modified | 6 |

## Acceptance Criteria Results

| Criterion | Status | Notes |
|-----------|--------|-------|
| AC-1: Branded splash appears on load | Pass | SplashScreen renders during `loading \|\| !splashDone` |
| AC-2: Splash exits smoothly | Pass | GSAP fades out after timeline completes via onComplete |
| AC-3: Build passes with gsap | Pass | gsap ^3.14.2 in dependencies, build exit 0 |
| AC-4: Landing CTA buttons work | Pass | All 6 CTAs use `to={ROUTES.REPORTS}`, no redirect loop |
| AC-5: Landing logo is GDO SVG | Pass | `/brand/logo.svg` in navbar, emoji text removed |
| AC-6: Default brand color matches GDO | Pass | `#6366f1` in AppShell fallback + index.css --color-accent |
| AC-7: Reset-to-default button | Pass | "Resetiraj na zadano" button → `setBrandColor('#6366f1')` |
| AC-8: Letter spacing throughout app | Pass | `letter-spacing: 0.015em` on body, `tracking-wide` on nav labels |

## Accomplishments

- Installed gsap and built animated splash screen (fade-in + slide-up on logo + text, fade-out on complete)
- Fixed landing page redirect loop — CTAs now route to login/signup correctly
- Replaced emoji logo with GDO SVG in landing navbar
- Unified brand color to #6366f1 indigo across AppShell, company settings, and global CSS

## Task Commits

All tasks executed in APPLY phase; no individual task commits — single phase commit expected.

## Files Created/Modified

| File | Change | Purpose |
|------|--------|---------|
| `app/src/features/auth/components/SplashScreen.tsx` | Created | GSAP-animated full-screen splash on auth load |
| `app/package.json` | Modified | Added gsap ^3.14.2 |
| `app/src/features/auth/components/LoginView.tsx` | Modified | SplashScreen import, splashDone state, conditional render |
| `app/src/features/landing/LandingPage.tsx` | Modified | ROUTES import, all CTAs → ROUTES.REPORTS, SVG logo in navbar |
| `app/src/features/organizations/components/CompanySettingsView.tsx` | Modified | Default #6366f1, "Resetiraj na zadano" reset button |
| `app/src/app/layouts/AppShell.tsx` | Modified | Fallback #6366f1 on NavItem/MobileNavItem, tracking-wide on nav labels |
| `app/src/index.css` | Modified | --color-accent #6366f1, letter-spacing 0.015em on body |

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| CTAs → ROUTES.REPORTS (not /login) | No dedicated /login route; catchall → App → LoginView is the auth entry | Future phases adding /login route should review landing CTAs |
| #3b82f6 left in other components (Dashboard, Calendar, etc.) | Out of scope for this plan; scoped to AppShell + CompanySettings only | Deferred color harmonization across all components |
| SplashScreen only in LoginView, not per-page | Scope limit defined in plan: initial load only | Per-page transitions remain unstyled |

## Deviations from Plan

### Summary

| Type | Count | Impact |
|------|-------|--------|
| Auto-fixed | 0 | None |
| Scope additions | 0 | None |
| Deferred | 1 | Cosmetic only |

### Deferred Items

- Color harmonization: ~10 other components still use `brandColor || '#3b82f6'` as fallback (DashboardView, ReportsView, CalendarView, ProjectsView, MasterProjects, etc.). These were explicitly out of scope for this plan. The AppShell and CompanySettings changes cover the most-visible surfaces; the rest remain blue until a future color-harmony pass.

## Issues Encountered

None — plan executed cleanly.

## Next Phase Readiness

**Ready:**
- Phase 18.1 complete: all brand identity work shipped
- App builds (exit 0) with gsap included
- Splash screen provides polished first impression on live deploy
- Landing page is fully functional (CTAs work, no redirect loops)

**Concerns:**
- Other components (Dashboard, Calendar, PDF generator) still use `#3b82f6` fallback — visually inconsistent if brand color is unset
- Splash screen re-renders on every `loading=true` cycle (including token refresh) — acceptable for MVP but could be optimized later

**Blockers:**
- None

---
*Phase: 18.1-brand-identity, Plan: 02*
*Completed: 2026-03-31*
