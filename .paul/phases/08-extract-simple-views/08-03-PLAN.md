---
phase: 08-extract-simple-views
plan: 03
type: execute
wave: 1
depends_on: ["08-02"]
files_modified:
  - app/src/features/calendar/components/CalendarView.tsx
  - app/src/features/reports/components/ReportsView.tsx
  - app/src/features/masterProjects/components/MasterProjectsListView.tsx
  - app/src/features/masterProjects/components/MasterProjectDetailView.tsx
  - app/src/App.tsx
autonomous: true
---

<objective>
## Goal
Extract CalendarView, ReportsView, MasterProjectsListView, and MasterProjectDetailView from App.tsx into feature folders. Close Phase 8.

## Purpose
Last 4 simple views still inline in App.tsx. After this plan, Phase 8 is complete: all 12 prop-driven view components extracted. App.tsx shrinks by ~1087 lines (4207 → ~3120).

## Output
- `app/src/features/calendar/components/CalendarView.tsx` (created)
- `app/src/features/reports/components/ReportsView.tsx` (created)
- `app/src/features/masterProjects/components/MasterProjectsListView.tsx` (created)
- `app/src/features/masterProjects/components/MasterProjectDetailView.tsx` (created)
- `app/src/App.tsx` (modified — 4 imports added, 4 inline defs removed)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/08-extract-simple-views/08-02-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: CalendarView extracted
```gherkin
Given App.tsx contains CalendarView defined inline (lines 2303–2598, ~296 lines)
When the extract-and-import pattern is applied
Then CalendarView exists in features/calendar/components/CalendarView.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-2: ReportsView extracted
```gherkin
Given App.tsx contains ReportsView defined inline (lines 2599–3038, ~440 lines)
When the extract-and-import pattern is applied
Then ReportsView exists in features/reports/components/ReportsView.tsx,
     App.tsx imports it from that path,
     the inline definition is removed from App.tsx,
     and npm run build exits 0
```

## AC-3: MasterProjectsListView + MasterProjectDetailView extracted
```gherkin
Given App.tsx contains both views defined inline (lines 3856–4207, ~352 lines)
When the extract-and-import pattern is applied
Then both components exist in features/masterProjects/components/,
     App.tsx imports both from those paths,
     the inline definitions are removed from App.tsx,
     and npm run build exits 0
```

## AC-4: Build integrity maintained throughout
```gherkin
Given Phase 8 governs constraint — nothing gets broken
When each extraction step completes
Then npm run build exits 0 after each of: (a) file created, (b) import added, (c) inline removed
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract CalendarView</name>
  <files>app/src/features/calendar/components/CalendarView.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/calendar/components/CalendarView.tsx`.
    Copy the CalendarView function body verbatim from App.tsx lines 2303–2597.
    Add all necessary imports at the top of the new file:
    - `import React, { useState } from 'react';`
    - `import { Card } from '../../../shared/ui';`
    - `import type { Project, DiaryEntry, CalendarEvent } from '../../../shared/types';`
    - date-fns: `format, startOfMonth, endOfMonth, startOfWeek, endOfWeek, eachDayOfInterval, isSameMonth, isSameDay, addMonths, subMonths, addWeeks, subWeeks, addDays, subDays, parseISO, isToday, startOfDay, endOfDay`
    - `import { hr } from 'date-fns/locale';`
    - Lucide icons used: inspect the function body and import only what appears — likely `ChevronRight, ExternalLink, Calendar, CalendarDays, Plus, X` (check actual usage and import only those)
    Export as `export default function CalendarView`.

    **Step 2 — Build check (import added):**
    Add to App.tsx imports (after the NewProjectView import line):
    `import CalendarView from './features/calendar/components/CalendarView';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition:**
    Delete lines 2303–2598 (the CalendarView function body including trailing blank line) from App.tsx using a Node.js script.
    Run `npm run build` — must exit 0.

    **Avoid:** Removing lines before confirming import works. Never delete before build passes.
  </action>
  <verify>
    - `grep -n "^function CalendarView" app/src/App.tsx` → 0 results
    - `grep -n "CalendarView" app/src/App.tsx` → only the import line + call site(s)
    - `npm run build` exits 0
  </verify>
  <done>AC-1 satisfied: CalendarView in features/calendar/components/CalendarView.tsx, imported, inline removed, build green</done>
</task>

<task type="auto">
  <name>Task 2: Extract ReportsView</name>
  <files>app/src/features/reports/components/ReportsView.tsx, app/src/App.tsx</files>
  <action>
    **Step 1 — Create the file:**
    Create `app/src/features/reports/components/ReportsView.tsx`.
    Copy the ReportsView function body verbatim from App.tsx (starts with `function ReportsView`, ends at the closing `}` before the `SUGGESTED_MATERIALS` const — do NOT include SUGGESTED_MATERIALS, which belongs to NewEntryView).
    Add necessary imports at the top:
    - `import React, { useState, useMemo } from 'react';`
    - `import { Card, Button, Input } from '../../../shared/ui';`
    - `import type { Project, DiaryEntry, Company } from '../../../shared/types';`
    - date-fns: `format, startOfMonth, endOfMonth, subMonths, parseISO, subDays, startOfDay, endOfDay`
    - `import { jsPDF } from 'jspdf';`
    - `import autoTable from 'jspdf-autotable';`
    - recharts: `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend, Cell, PieChart, Pie`
    - Lucide icons: inspect the function body and import only what appears (likely `Download, FileText, BarChart2` or similar — check actual usage)
    Export as `export default function ReportsView`.

    **Step 2 — Build check (import added):**
    Add to App.tsx imports:
    `import ReportsView from './features/reports/components/ReportsView';`
    Run `npm run build` — must exit 0.

    **Step 3 — Remove inline definition:**
    Delete the ReportsView function body from App.tsx (from `function ReportsView` through its closing `}`, stopping before the blank line before `const SUGGESTED_MATERIALS`).
    Run `npm run build` — must exit 0.

    **Avoid:** Including SUGGESTED_MATERIALS in the extracted file — it belongs to NewEntryView and must stay in App.tsx for Phase 9.
  </action>
  <verify>
    - `grep -n "^function ReportsView" app/src/App.tsx` → 0 results
    - `grep -n "SUGGESTED_MATERIALS" app/src/App.tsx` → still present (not removed)
    - `npm run build` exits 0
  </verify>
  <done>AC-2 satisfied: ReportsView in features/reports/components/ReportsView.tsx, imported, inline removed, SUGGESTED_MATERIALS untouched, build green</done>
</task>

<task type="auto">
  <name>Task 3: Extract MasterProjectsListView and MasterProjectDetailView</name>
  <files>
    app/src/features/masterProjects/components/MasterProjectsListView.tsx,
    app/src/features/masterProjects/components/MasterProjectDetailView.tsx,
    app/src/App.tsx
  </files>
  <action>
    Both views share the `masterProjects` domain and can be extracted together.

    **Step 1 — Create MasterProjectsListView:**
    Create `app/src/features/masterProjects/components/MasterProjectsListView.tsx`.
    Copy the MasterProjectsListView function verbatim from App.tsx (~lines 3856–3925).
    Imports needed:
    - `import React from 'react';`
    - `import type { MasterProject } from '../../../lib/data';`
    - Lucide icons used in the component (likely `Plus, Layers, ChevronRight, MapPin` — check actual usage)
    Export as `export default function MasterProjectsListView`.

    **Step 2 — Create MasterProjectDetailView:**
    Create `app/src/features/masterProjects/components/MasterProjectDetailView.tsx`.
    Copy the MasterProjectDetailView function verbatim from App.tsx (~lines 3927–4207, the last function in the file).
    Imports needed:
    - `import React from 'react';`
    - `import { Button, Input, Card } from '../../../shared/ui';`
    - `import { cn } from '../../../lib/utils';`
    - `import type { MasterProject, MasterProjectOrganization, MasterProjectStats, MasterActivityItem, MasterProjectIssue } from '../../../lib/data';`
    - Lucide icons: inspect the function body and import only what appears
    Export as `export default function MasterProjectDetailView`.

    **Step 3 — Build check (imports added):**
    Add both imports to App.tsx:
    ```
    import MasterProjectsListView from './features/masterProjects/components/MasterProjectsListView';
    import MasterProjectDetailView from './features/masterProjects/components/MasterProjectDetailView';
    ```
    Run `npm run build` — must exit 0.

    **Step 4 — Remove both inline definitions:**
    Delete the MasterProjectsListView and MasterProjectDetailView function bodies from App.tsx. Since MasterProjectDetailView is the last function in the file, this reduces App.tsx to end after the NewEntryView closing brace.
    Run `npm run build` — must exit 0.

    **Avoid:** Removing the `// ─── Master Workspace Views ───` comment without checking if it aids readability — it can stay or go, but must not break surrounding context.
  </action>
  <verify>
    - `grep -n "^function MasterProjectsListView\|^function MasterProjectDetailView" app/src/App.tsx` → 0 results
    - `grep -n "MasterProjectsListView\|MasterProjectDetailView" app/src/App.tsx` → only import lines + call sites
    - `wc -l app/src/App.tsx` → approximately 3120 lines (was 4207)
    - `npm run build` exits 0
  </verify>
  <done>AC-3 and AC-4 satisfied: both master views extracted, imported, inlines removed, build green, Phase 8 complete</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `app/src/App.tsx` function signatures at call sites — only add imports and remove inline definitions
- `SUGGESTED_MATERIALS` const (lines ~3040–3054) — belongs to NewEntryView, extracted in Phase 9
- `NewEntryView`, `DiaryEntryDetailModal`, `ProjectDetailView`, `ProjectMembersTab`, `ProjectTasksTab`, `ProjectDocumentsTab`, `PhotoGallery` — Phase 9 scope, do not touch
- All files in `supabase/migrations/` — locked
- Existing behavior of all views — no logic changes, verbatim extraction

## SCOPE LIMITS
- No typing improvements (all `any` typing stays — Phase 10 will tighten)
- No Lucide icon pruning in App.tsx imports — deferred until all views extracted
- No restructuring of component internals — copy verbatim
- Do not extract NewEntryView even though it sits between ReportsView and MasterProjectsListView

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "^function CalendarView\|^function ReportsView\|^function MasterProjectsListView\|^function MasterProjectDetailView" app/src/App.tsx` → 0 results
- [ ] All 4 new files exist in their feature folders
- [ ] `npm run build` exits 0 (final build)
- [ ] `wc -l app/src/App.tsx` → ~3120 lines (was 4207, reduction of ~1087)
- [ ] `SUGGESTED_MATERIALS` still present in App.tsx
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 4 views extracted and imported
- App.tsx reduced by ~1087 lines (4207 → ~3120)
- `npm run build` passes throughout (after each create, each import, each removal)
- No behavior changes — verbatim extraction
- Phase 8 complete: all 12 simple view components extracted from App.tsx
</success_criteria>

<output>
After completion, create `.paul/phases/08-extract-simple-views/08-03-SUMMARY.md`
</output>
