---
phase: 08-extract-simple-views
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - app/src/App.tsx
  - app/src/integrations/pdf/generateDiaryPdf.ts
  - app/src/features/dashboard/components/DashboardView.tsx
  - app/src/features/projects/components/ProjectsView.tsx
  - app/src/features/projects/components/NewProjectView.tsx
autonomous: true
---

<objective>
## Goal
Extract `generatePDF`, `DashboardView`, `ProjectsView`, and `NewProjectView` from App.tsx into feature/integration folders; reduce App.tsx by ~490 lines.

## Purpose
Continues Phase 8 extraction pattern. After this plan, the `dashboard` and `projects` feature domains are established, the PDF generation logic lives in a dedicated integration module, and App.tsx is ~4380 lines.

## Output
- `app/src/integrations/pdf/generateDiaryPdf.ts` (new)
- `app/src/features/dashboard/components/DashboardView.tsx` (new)
- `app/src/features/projects/components/ProjectsView.tsx` (new)
- `app/src/features/projects/components/NewProjectView.tsx` (new)
- `app/src/App.tsx` (modified — 4 imports added, 4 definitions removed)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/08-extract-simple-views/08-01-SUMMARY.md

## Source Files
@app/src/App.tsx
</context>

<acceptance_criteria>

## AC-1: PDF generation extracted
```gherkin
Given generatePDF is defined at App.tsx line 645
When extracted to src/integrations/pdf/generateDiaryPdf.ts as a named export
Then App.tsx imports it and calls it identically; build passes
```

## AC-2: DashboardView extracted
```gherkin
Given DashboardView is defined at App.tsx line 1437
When extracted to src/features/dashboard/components/DashboardView.tsx
Then App.tsx imports it and renders it identically; build passes
```

## AC-3: ProjectsView and NewProjectView extracted
```gherkin
Given ProjectsView (line 1687) and NewProjectView (line 1850) are defined in App.tsx
When extracted to src/features/projects/components/
Then App.tsx imports both and renders them identically; build passes
```

## AC-4: Build integrity maintained
```gherkin
Given the extract-and-import pattern (create → import → build → remove inline → build)
When each task's final npm run build is executed
Then it exits 0 with no type errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extract generatePDF to integrations/pdf/generateDiaryPdf.ts</name>
  <files>app/src/integrations/pdf/generateDiaryPdf.ts, app/src/App.tsx</files>
  <action>
    1. Create `app/src/integrations/pdf/` directory.
    2. Create `app/src/integrations/pdf/generateDiaryPdf.ts`:
       - Import `jsPDF` from 'jspdf' and `autoTable` from 'jspdf-autotable'
       - Import `Project` and `DiaryEntry` from '../../shared/types'
       - Copy the `generatePDF` function body from App.tsx lines 645–817 verbatim
       - Export it as: `export const generateDiaryPdf = async (project: Project, entries: DiaryEntry[]): Promise<void> => { ... }`
       - Note: the function uses `format` from date-fns and `safeFormatDate` from shared/utils/format — import both
    3. In App.tsx:
       - Add import at top (after existing feature imports): `import { generateDiaryPdf } from './integrations/pdf/generateDiaryPdf';`
       - Find every call to `generatePDF(` in AppContent and rename to `generateDiaryPdf(`
    4. Run `npm run build` — must exit 0
    5. Remove the `const generatePDF = async ...` definition from App.tsx (lines 645–817)
    6. Run `npm run build` again — must exit 0
    Avoid: renaming any parameters inside the function; keep logic 100% identical
  </action>
  <verify>
    grep -n "generatePDF\b" app/src/App.tsx → 0 results (old name gone)
    grep -n "generateDiaryPdf" app/src/App.tsx → import line + call sites only
    npm run build → exits 0
  </verify>
  <done>AC-1 and AC-4 satisfied: generateDiaryPdf.ts created, App.tsx updated, build green</done>
</task>

<task type="auto">
  <name>Task 2: Extract DashboardView to features/dashboard/components/DashboardView.tsx</name>
  <files>app/src/features/dashboard/components/DashboardView.tsx, app/src/App.tsx</files>
  <action>
    1. Create `app/src/features/dashboard/components/` directory.
    2. Create `app/src/features/dashboard/components/DashboardView.tsx`:
       - Add: `import React, { } from 'react';` (check if useState is used — it is not in DashboardView, but verify)
       - Import recharts used by DashboardView: `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell, PieChart, Pie` from 'recharts'
       - Import `format` from 'date-fns'
       - Import `Button, Card` from '../../../shared/ui'
       - Import `Project, DiaryEntry, Company, AppUser, GoogleTokens, CalendarEvent` from '../../../shared/types'
       - Copy the full `DashboardView` function (lines 1437–1685) verbatim
       - Add `export default DashboardView;` at the end
    3. In App.tsx add import: `import DashboardView from './features/dashboard/components/DashboardView';`
    4. Run `npm run build` — must exit 0
    5. Remove the `function DashboardView` definition from App.tsx (lines 1437–1685)
    6. Run `npm run build` again — must exit 0
    Avoid: changing any logic, props, or JSX; keep `any` typing as-is
  </action>
  <verify>
    grep -n "^function DashboardView" app/src/App.tsx → 0 results
    grep -n "DashboardView" app/src/App.tsx → import line + JSX usage only
    npm run build → exits 0
  </verify>
  <done>AC-2 and AC-4 satisfied: DashboardView.tsx created in features/dashboard/, build green</done>
</task>

<task type="auto">
  <name>Task 3: Extract ProjectsView and NewProjectView to features/projects/components/</name>
  <files>app/src/features/projects/components/ProjectsView.tsx, app/src/features/projects/components/NewProjectView.tsx, app/src/App.tsx</files>
  <action>
    1. Create `app/src/features/projects/components/` directory.
    2. Create `app/src/features/projects/components/ProjectsView.tsx`:
       - Import React, useState
       - Import `format` from 'date-fns'
       - Import `Button, Card, StatusBadge` from '../../../shared/ui'
       - Import `Project, Company` from '../../../shared/types'
       - Import `getPhases` from '../../../lib/disciplineConfig' (if used — check body)
       - Copy `function ProjectsView` (lines 1687–1848) verbatim
       - Add `export default ProjectsView;`
    3. Create `app/src/features/projects/components/NewProjectView.tsx`:
       - Import React, useState
       - Import `format` from 'date-fns'
       - Import `Button, Card, Input, Select` from '../../../shared/ui'
       - Import `Company` from '../../../shared/types'
       - Import `getPhases` from '../../../lib/disciplineConfig' (if used — check body)
       - Copy `function NewProjectView` (lines 1850–1940) verbatim
       - Add `export default NewProjectView;`
    4. In App.tsx add two imports:
       - `import ProjectsView from './features/projects/components/ProjectsView';`
       - `import NewProjectView from './features/projects/components/NewProjectView';`
    5. Run `npm run build` — must exit 0
    6. Remove both `function ProjectsView` (lines 1687–1848) and `function NewProjectView` (lines 1850–1940) from App.tsx
    7. Run `npm run build` again — must exit 0
    Avoid: changing any logic, props, or JSX; keep `any` typing as-is
  </action>
  <verify>
    grep -n "^function ProjectsView\|^function NewProjectView" app/src/App.tsx → 0 results
    grep -n "ProjectsView\|NewProjectView" app/src/App.tsx → import lines + JSX usage only
    wc -l app/src/App.tsx → approximately 4380–4400 lines
    npm run build → exits 0
  </verify>
  <done>AC-3 and AC-4 satisfied: both project views extracted, App.tsx ~490 lines shorter, build green</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `app/src/features/auth/` — extracted in 08-01, do not touch
- `app/src/features/users/` — extracted in 08-01, do not touch
- `app/src/features/organizations/` — extracted in 08-01, do not touch
- `app/src/shared/` — no changes to shared types, utils, or UI primitives
- `supabase/migrations/` — schema is locked

## SCOPE LIMITS
- Do not extract any other functions (ProjectDetailView, CalendarView, ReportsView etc. — those are 08-03)
- Do not change function logic, props, or JSX in extracted components
- Do not tighten `any` prop types — deferred to Phase 10
- Do not prune unused lucide icon imports from App.tsx — deferred until all views extracted
- Do not add barrel index.ts files unless naturally needed for the import

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -n "^function DashboardView\|^function ProjectsView\|^function NewProjectView" app/src/App.tsx` → 0 results
- [ ] `grep -n "generatePDF\b" app/src/App.tsx` → 0 results
- [ ] `grep -n "generateDiaryPdf\|DashboardView\|ProjectsView\|NewProjectView" app/src/App.tsx` → only import lines + usage
- [ ] `npm run build` exits 0
- [ ] `wc -l app/src/App.tsx` → ~4380–4400 lines (down from 4883)
- [ ] All 4 new files exist and contain the extracted code
</verification>

<success_criteria>
- All 4 files created in correct locations
- App.tsx reduced by ~490 lines
- Build passes green at every intermediate step
- No behavior change — pure extraction
</success_criteria>

<output>
After completion, create `.paul/phases/08-extract-simple-views/08-02-SUMMARY.md`
</output>
